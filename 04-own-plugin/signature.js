const register = () => {};

const name = 'signature-plugin';

const version = '1.0.0';

const multiple = false;

const dependencies = '';

const once = true;

const pkg = {};

exports.plugin = { register, name, version, multiple, dependencies, once, pkg };
